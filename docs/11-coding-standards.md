# Coding Standards

TypeScript strict mode; ESLint + Prettier; readable code over clever abstraction; justify dependencies; no hardcoded roles; no Prisma in controllers; validate inputs; migrations for DB changes; tests for business logic; no unrelated modifications; generic shared packages only; no premature microservices/abstraction. Fix task-caused lint/typecheck/test/build failures before REVIEW.
