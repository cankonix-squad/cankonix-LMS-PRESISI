const integrationPage=document.body.dataset.integrationPage,integrationRoot=document.querySelector("#integrationContent");
let integration=getIntegrationData();
function integrationReload(){integration=getIntegrationData();renderIntegrationPage()}
function integrationHero(title,copy,action=""){return`<div class="hero"><div class="page-actions" style="justify-content:space-between"><div><h1>${escapeHtml(title)}</h1><p>${escapeHtml(copy)}</p></div>${action}</div></div>`}
function connectorById(id){return integration.connectors.find(item=>item.id===id)}
function integrationBadge(status){const color=["Healthy","Completed","Active","Resolved","Published"].includes(status)?"green":["Error","Failed","Open"].includes(status)?"red":"orange";return`<span class="badge ${color}">${escapeHtml(status)}</span>`}
function addConnector(){openEducationModal("Tambah Connector",`<div class="form-grid"><div class="form-row full"><label>Nama connector</label><input name="name" required></div><div class="form-row"><label>Tipe</label><select name="type"><option>REST API</option><option>DB Read-only</option><option>File/Batch</option><option>Webhook</option><option>Event</option><option>SSO</option><option>CCTV</option></select></div><div class="form-row"><label>Data domain</label><input name="domain" required></div><div class="form-row full"><label>Endpoint/source</label><input name="endpoint" required></div><div class="form-row"><label>Authentication</label><select name="auth"><option>OAuth 2.0</option><option>Service Account</option><option>mTLS</option><option>API Key</option><option>SSH Key</option></select></div><div class="form-row"><label>Schedule</label><input name="schedule" value="Setiap 1 jam" required></div></div>`,form=>{integration.connectors.push({id:educationId("conn"),...Object.fromEntries(form),status:"Warning",paused:false,lastSync:"Belum pernah",records:0,success:0,errors:0,latency:0});saveIntegrationData(integration);integrationReload();showEducationToast("Connector dibuat dalam status konfigurasi.")})}
function configureConnector(id){const item=connectorById(id);openEducationModal("Konfigurasi Connector",`<div class="form-grid"><div class="form-row full"><label>Endpoint/source</label><input name="endpoint" value="${escapeHtml(item.endpoint)}" required></div><div class="form-row"><label>Authentication</label><input name="auth" value="${escapeHtml(item.auth)}" required></div><div class="form-row"><label>Schedule</label><input name="schedule" value="${escapeHtml(item.schedule)}" required></div></div>`,form=>{Object.assign(item,Object.fromEntries(form));item.status="Healthy";saveIntegrationData(integration);integrationReload();showEducationToast("Konfigurasi divalidasi.")})}
function toggleConnector(id){const item=connectorById(id);item.paused=!item.paused;item.status=item.paused?"Paused":"Healthy";saveIntegrationData(integration);integrationReload()}
function runConnector(id){const item=connectorById(id);if(item.paused){alert("Connector sedang paused.");return}const now=new Date().toISOString().replace("T"," ").slice(0,16),records=Math.max(1,item.records||100),rejected=item.status==="Error"?Math.max(1,item.errors):0;integration.runs.unshift({id:educationId("run"),connectorId:id,startedAt:now,endedAt:now,status:rejected?"Completed with Error":"Completed",records,accepted:records-rejected,rejected,logs:["Connection established",`Mapping loaded for ${item.domain}`,`${records-rejected} records accepted`,`${rejected} records rejected`],rejectedRecords:rejected?[{id:educationId("rej"),sourceId:`${item.domain.toUpperCase()}-${String(Date.now()).slice(-4)}`,reason:"Validation rule failed",status:"Open"}]:[]});item.lastSync=now;item.success=records-rejected;item.errors=rejected;item.status=rejected?"Warning":"Healthy";item.latency=Math.max(80,Math.round((item.latency||200)*.8));saveIntegrationData(integration);integrationReload();showEducationToast("Sync run selesai.")}
function retryRejected(runId,rejectedId){const run=integration.runs.find(item=>item.id===runId),record=run.rejectedRecords.find(item=>item.id===rejectedId);record.status="Retried";run.rejected=run.rejectedRecords.filter(item=>item.status==="Open").length;run.accepted=run.records-run.rejected;run.logs.push(`${record.sourceId} retried and accepted`);if(run.rejected===0)run.status="Completed";saveIntegrationData(integration);integrationReload()}
function renderIntegrationDashboard(){const totalRecords=integration.connectors.reduce((sum,item)=>sum+item.records,0),errors=integration.connectors.reduce((sum,item)=>sum+item.errors,0),healthy=integration.connectors.filter(item=>item.status==="Healthy").length,avgLatency=Math.round(integration.connectors.reduce((sum,item)=>sum+item.latency,0)/integration.connectors.length);integrationRoot.innerHTML=integrationHero("Integration Hub","Monitoring connector, sync run, error, latency, dan retry.",'<a class="btn btn-light" href="connector-catalog.html">Connector Catalog</a>')+`<div class="grid grid-4"><div class="card kpi"><div class="kpi-label">Connector Healthy</div><div class="kpi-value">${healthy}/${integration.connectors.length}</div></div><div class="card kpi"><div class="kpi-label">Records</div><div class="kpi-value">${totalRecords.toLocaleString("id-ID")}</div></div><div class="card kpi"><div class="kpi-label">Error</div><div class="kpi-value">${errors}</div></div><div class="card kpi"><div class="kpi-label">Avg Latency</div><div class="kpi-value">${avgLatency}ms</div></div></div><div class="connector-grid">${integration.connectors.map(item=>`<article class="card connector-card"><div class="connector-head"><span class="connector-icon">${escapeHtml(item.type.slice(0,3).toUpperCase())}</span>${integrationBadge(item.status)}</div><h2>${escapeHtml(item.name)}</h2><p>${escapeHtml(item.domain)} • ${escapeHtml(item.type)}</p><div class="connector-metrics"><span>Last sync<b>${escapeHtml(item.lastSync)}</b></span><span>Success<b>${item.success.toLocaleString("id-ID")}</b></span><span>Error<b>${item.errors}</b></span><span>Latency<b>${item.latency}ms</b></span></div><div class="row-actions"><button onclick="runConnector('${item.id}')">Run Now</button><button onclick="toggleConnector('${item.id}')">${item.paused?"Resume":"Pause"}</button></div></article>`).join("")}</div><section class="card"><div class="section-title">Sync Run & Rejected Records</div>${integration.runs.map(run=>`<div class="sync-run"><div class="sync-summary"><span><b>${escapeHtml(connectorById(run.connectorId)?.name||"-")}</b><small>${escapeHtml(run.startedAt)} • ${run.accepted}/${run.records} accepted</small></span>${integrationBadge(run.status)}</div><div class="sync-logs">${run.logs.map(log=>`<code>${escapeHtml(log)}</code>`).join("")}</div>${run.rejectedRecords.map(record=>`<div class="rejected-row"><span><b>${escapeHtml(record.sourceId)}</b><small>${escapeHtml(record.reason)}</small></span>${integrationBadge(record.status)}<button ${record.status!=="Open"?"disabled":""} onclick="retryRejected('${run.id}','${record.id}')">Retry</button></div>`).join("")}</div>`).join("")}</section>`}
function renderConnectorCatalog(){integrationRoot.innerHTML=integrationHero("Connector Catalog","REST API, database, batch, webhook, event, SSO, dan CCTV.",'<button class="btn btn-light" onclick="addConnector()">+ Connector</button>')+`<div class="grid grid-3">${integration.connectors.map(item=>`<article class="card catalog-card"><div class="connector-head"><span class="connector-icon">${escapeHtml(item.type.slice(0,3).toUpperCase())}</span>${integrationBadge(item.status)}</div><span class="eyebrow dark">${escapeHtml(item.type)} • ${escapeHtml(item.domain)}</span><h2>${escapeHtml(item.name)}</h2><dl><dt>Endpoint</dt><dd>${escapeHtml(item.endpoint)}</dd><dt>Auth</dt><dd>${escapeHtml(item.auth)}</dd><dt>Schedule</dt><dd>${escapeHtml(item.schedule)}</dd></dl><div class="row-actions"><button onclick="configureConnector('${item.id}')">Configure</button><a href="data-mapping.html?connector=${item.id}">Mapping →</a></div></article>`).join("")}</div>`}
function addMappingField(mappingId){const mapping=integration.mappings.find(item=>item.id===mappingId);openEducationModal("Tambah Mapping Field",`<div class="form-grid"><div class="form-row"><label>Source field</label><input name="source" required></div><div class="form-row"><label>Canonical target</label><input name="target" required></div><div class="form-row"><label>Transform</label><input name="transform" value="direct" required></div><div class="form-row"><label>Sample</label><input name="sample"></div><label class="check-row"><input name="required" type="checkbox"> Required field</label></div>`,form=>{const value=Object.fromEntries(form);mapping.fields.push({...value,required:value.required==="on"});mapping.status="Draft";saveIntegrationData(integration);integrationReload()})}
function publishMapping(id){const mapping=integration.mappings.find(item=>item.id===id);mapping.version=(Number.parseFloat(mapping.version)+.1).toFixed(1);mapping.status="Published";saveIntegrationData(integration);integrationReload();showEducationToast(`Mapping v${mapping.version} dipublikasikan.`)}
function renderDataMapping(){const connectorId=new URLSearchParams(location.search).get("connector"),rows=integration.mappings.filter(item=>!connectorId||item.connectorId===connectorId);integrationRoot.innerHTML=integrationHero("Data Mapping","Pemetaan source field menuju canonical education data model.",'<a class="btn btn-light" href="connector-catalog.html">Connector Catalog</a>')+`<div class="mapping-list">${rows.map(mapping=>`<section class="card"><div class="section-actions"><div><span class="eyebrow dark">${escapeHtml(connectorById(mapping.connectorId)?.name||"-")}</span><h2>Mapping v${escapeHtml(mapping.version)}</h2></div>${integrationBadge(mapping.status)}</div><div class="table-wrap"><table class="table"><thead><tr><th>Source</th><th></th><th>Canonical Target</th><th>Transform</th><th>Required</th><th>Sample</th></tr></thead><tbody>${mapping.fields.map(field=>`<tr><td><code>${escapeHtml(field.source)}</code></td><td>→</td><td><code>${escapeHtml(field.target)}</code></td><td>${escapeHtml(field.transform)}</td><td>${field.required?"Ya":"Tidak"}</td><td>${escapeHtml(field.sample)}</td></tr>`).join("")}</tbody></table></div><div class="mapping-actions"><button class="btn btn-light" onclick="addMappingField('${mapping.id}')">+ Field</button><button class="btn btn-primary" onclick="publishMapping('${mapping.id}')">Publish Version</button></div></section>`).join("")||'<div class="card empty-state">Mapping belum tersedia untuk connector ini.</div>'}</div>`}
function resolveConflict(id,choice){const item=integration.conflicts.find(value=>value.id===id);item.resolution=choice==="A"?item.sourceA:item.sourceB;item.status="Resolved";saveIntegrationData(integration);integrationReload()}
function renderDataQuality(){const avg=metric=>Math.round(integration.quality.reduce((sum,item)=>sum+item[metric],0)/integration.quality.length);integrationRoot.innerHTML=integrationHero("Education Data Hub","Data quality, lineage, freshness, validity, dan conflict resolution.",'<a class="btn btn-light" href="integration-dashboard.html">Integration Hub</a>')+`<div class="grid grid-4"><div class="card kpi"><div class="kpi-label">Completeness</div><div class="kpi-value">${avg("completeness")}%</div></div><div class="card kpi"><div class="kpi-label">Uniqueness</div><div class="kpi-value">${100-avg("duplication")}%</div></div><div class="card kpi"><div class="kpi-label">Freshness</div><div class="kpi-value">${avg("freshness")}%</div></div><div class="card kpi"><div class="kpi-label">Validity</div><div class="kpi-value">${avg("validity")}%</div></div></div><section class="card"><div class="section-title">Quality by Domain</div><div class="quality-domain-grid">${integration.quality.map(item=>`<div class="quality-domain"><b>${escapeHtml(item.domain)}</b>${[["Completeness",item.completeness],["Duplication",100-item.duplication],["Freshness",item.freshness],["Validity",item.validity]].map(metric=>`<span><small>${metric[0]}</small><div class="progress"><i style="width:${metric[1]}%"></i></div><strong>${metric[1]}%</strong></span>`).join("")}</div>`).join("")}</div></section><section class="card"><div class="section-title">Record Lineage</div><div class="table-wrap"><table class="table"><thead><tr><th>Canonical Record</th><th>Source System</th><th>Source ID</th><th>Sync Time</th><th>Mapping</th><th>Status</th></tr></thead><tbody>${integration.lineage.map(item=>`<tr><td><code>${escapeHtml(item.canonicalId)}</code></td><td>${escapeHtml(item.sourceSystem)}</td><td>${escapeHtml(item.sourceId)}</td><td>${escapeHtml(item.syncTime)}</td><td>v${escapeHtml(item.mappingVersion)} → ${escapeHtml(item.target)}</td><td>${integrationBadge(item.status)}</td></tr>`).join("")}</tbody></table></div></section><section class="card"><div class="section-title">Conflict Resolution</div>${integration.conflicts.map(item=>`<div class="conflict-row"><span><b>${escapeHtml(item.entity)} • ${escapeHtml(item.field)}</b><small>${escapeHtml(item.sourceAName)}: ${escapeHtml(item.sourceA)} ↔ ${escapeHtml(item.sourceBName)}: ${escapeHtml(item.sourceB)}</small></span>${integrationBadge(item.status)}${item.status==="Open"?`<div class="row-actions"><button onclick="resolveConflict('${item.id}','A')">Pilih ${escapeHtml(item.sourceA)}</button><button onclick="resolveConflict('${item.id}','B')">Pilih ${escapeHtml(item.sourceB)}</button></div>`:`<b>${escapeHtml(item.resolution)}</b>`}</div>`).join("")}</section>`}
const integrationManager=["adminti","adminpusat"].includes(getRole());
function guardIntegrationMutation(handler){return function(...args){if(!integrationManager){alert("Akses perubahan integrasi hanya tersedia untuk Admin TI atau Admin Pusat.");return}return handler(...args)}}
addConnector=guardIntegrationMutation(addConnector);
configureConnector=guardIntegrationMutation(configureConnector);
toggleConnector=guardIntegrationMutation(toggleConnector);
runConnector=guardIntegrationMutation(runConnector);
retryRejected=guardIntegrationMutation(retryRejected);
addMappingField=guardIntegrationMutation(addMappingField);
publishMapping=guardIntegrationMutation(publishMapping);
resolveConflict=guardIntegrationMutation(resolveConflict);
function renderIntegrationPage(){const labels={dashboard:["Integration Hub","Integration Hub"],connectors:["Source System","Connector Catalog"],mapping:["Data Mapping","Data Mapping"],quality:["Data Quality","Education Data Hub"]}[integrationPage];renderSidebar(labels[0]);renderHeader(labels[1],"Platform / Integration");({dashboard:renderIntegrationDashboard,connectors:renderConnectorCatalog,mapping:renderDataMapping,quality:renderDataQuality}[integrationPage]||renderIntegrationDashboard)();if(!integrationManager)integrationRoot.insertAdjacentHTML("afterbegin",'<div class="alert">Mode baca-saja. Perubahan integrasi hanya tersedia untuk Admin TI atau Admin Pusat.</div>')}
renderIntegrationPage();
if(!integrationManager)integrationRoot.querySelectorAll?.("button")?.forEach(button=>{button.disabled=true});
