const platformSeed = {
  master: [
    { id: "MD-001", group: "Organisasi", name: "Sekolah/Satdik", code: "SATDIK", version: "3.2", effective: "01-01-2026", status: "Berlaku" },
    { id: "MD-002", group: "Organisasi", name: "Satker", code: "SATKER", version: "2.4", effective: "01-01-2026", status: "Berlaku" },
    { id: "MD-003", group: "Organisasi", name: "Satwil", code: "SATWIL", version: "2.1", effective: "01-01-2026", status: "Berlaku" },
    { id: "MD-004", group: "Personel", name: "Pangkat Polri", code: "PANGKAT", version: "5.0", effective: "01-07-2026", status: "Berlaku" },
    { id: "MD-005", group: "Personel", name: "Golongan PNS", code: "GOL", version: "4.1", effective: "01-01-2026", status: "Berlaku" },
    { id: "MD-006", group: "Akademik", name: "Periode Akademik", code: "PERIODE", version: "1.8", effective: "01-08-2026", status: "Draft" },
    { id: "MD-007", group: "Governance", name: "Capability & Menu", code: "CAPABILITY", version: "2.0", effective: "01-09-2026", status: "Review" }
  ],
  users: [
    { id: "USR-001", name: "Kombes Pol. Arif Nugraha", nrp: "76080421", unit: "Lemdiklat Polri", role: "Admin Pusat", scope: "Nasional", mfa: "Aktif", status: "Aktif" },
    { id: "USR-002", name: "AKBP Maya Prameswari", nrp: "80051234", unit: "SPN-JBR", role: "Admin Satdik", scope: "SPN-JBR", mfa: "Aktif", status: "Aktif" },
    { id: "USR-003", name: "Kompol Dimas Saputra", nrp: "85061290", unit: "SPN-JBR", role: "Pengelola Akademik", scope: "SPN-JBR", mfa: "Aktif", status: "Aktif" },
    { id: "USR-004", name: "Penata I Nabila Putri", nrp: "199006122015", unit: "SPN-JBR", role: "Auditor Mutu", scope: "SPN-JBR", mfa: "Aktif", status: "Aktif" }
  ],
  policies: [
    { id: "POL-01", name: "Akses Data Satdik", model: "RBAC + ABAC", rule: "unit_id in assigned_units", status: "Published" },
    { id: "POL-02", name: "Catatan Pengasuhan Terbatas", model: "ABAC", rule: "role=pengasuh AND assigned_cohort", status: "Published" },
    { id: "POL-03", name: "Approval Master Sensitif", model: "Workflow", rule: "maker != approver", status: "Draft" }
  ],
  units: [
    { id: "ORG-01", name: "Lemdiklat Polri", parent: "-", leader: "Kalemdiklat Polri", positions: 18, status: "Aktif" },
    { id: "ORG-02", name: "Sekretariat Lemdiklat", parent: "Lemdiklat Polri", leader: "Seslem Lemdiklat", positions: 12, status: "Aktif" },
    { id: "ORG-03", name: "SPN-JBR", parent: "Lemdiklat Polri", leader: "Ka SPN Polda Jawa Barat", positions: 24, status: "Aktif" },
    { id: "ORG-04", name: "Bagian Akademik", parent: "SPN-JBR", leader: "Kabag Akademik", positions: 9, status: "Aktif" },
    { id: "ORG-05", name: "Bagian Pengasuhan", parent: "SPN-JBR", leader: "Kabag Pengasuhan", positions: 7, status: "Aktif" }
  ],
  personnel: [
    { id: "PER-001", name: "Kombes Pol. Arif Nugraha", nrp: "76080421", rank: "Kombes Pol.", unit: "Lemdiklat Polri", position: "Analis Kebijakan Utama", competency: "Manajemen Pendidikan", status: "Aktif" },
    { id: "PER-002", name: "AKBP Maya Prameswari", nrp: "80051234", rank: "AKBP", unit: "SPN-JBR", position: "Kabag Akademik", competency: "Desain Pembelajaran", status: "Aktif" },
    { id: "PER-003", name: "Kompol Dimas Saputra", nrp: "85061290", rank: "Kompol", unit: "SPN-JBR", position: "Gadik Utama", competency: "Pelatihan Kepolisian", status: "Aktif" },
    { id: "PER-004", name: "Penata I Nabila Putri", nrp: "199006122015", rank: "Penata I", unit: "SPN-JBR", position: "Auditor Mutu", competency: "Audit Pendidikan", status: "Aktif" }
  ],
  reports: [
    { id: "RPT-01", name: "Executive Summary Nasional", category: "Pimpinan", scope: "Nasional", updated: "05 Sep 2026 08:15", format: "PDF/XLSX" },
    { id: "RPT-02", name: "Rekap Program Pendidikan", category: "Akademik", scope: "Satdik", updated: "05 Sep 2026 07:30", format: "PDF/XLSX" },
    { id: "RPT-03", name: "Rekap Kehadiran Peserta", category: "Operasional", scope: "Kelas/Program", updated: "04 Sep 2026 21:10", format: "XLSX/CSV" },
    { id: "RPT-04", name: "Capaian 8 Standar", category: "Mutu", scope: "Nasional/Satdik", updated: "04 Sep 2026 18:45", format: "PDF" },
    { id: "RPT-05", name: "Sertifikasi dan Kompetensi", category: "LSP", scope: "Nasional/Satdik", updated: "03 Sep 2026 16:20", format: "PDF/XLSX" }
  ],
  schedules: [{ id: "SCH-01", report: "Executive Summary Nasional", frequency: "Bulanan", recipient: "Pimpinan Lemdiklat", next: "01 Okt 2026", status: "Aktif" }],
  audits: [
    { id: "AUD-260905-1042", time: "05 Sep 2026 10:42:18", actor: "demo.presisi", action: "UPDATE", object: "Program Diktuk Bintara Polri", module: "Program", ip: "10.20.1.24", result: "Sukses", correlation: "COR-7A91" },
    { id: "AUD-260905-1015", time: "05 Sep 2026 10:15:02", actor: "admin.pusat", action: "APPROVE", object: "Workspace SPN Jabar", module: "Provisioning", ip: "10.20.0.18", result: "Sukses", correlation: "COR-7A82" },
    { id: "AUD-260905-0948", time: "05 Sep 2026 09:48:33", actor: "sync.service", action: "RETRY", object: "Batch ABS-20260905", module: "Integration", ip: "service", result: "Warning", correlation: "COR-7A65" },
    { id: "AUD-260905-0912", time: "05 Sep 2026 09:12:10", actor: "auditor.mutu", action: "VIEW", object: "Evidence STD-04", module: "Quality", ip: "10.22.4.11", result: "Sukses", correlation: "COR-7A41" }
  ],
  services: [
    { name: "Frontend Portal", type: "Web", uptime: "99.99%", response: "184 ms", queue: "-", status: "Sehat" },
    { name: "Education Service", type: "API", uptime: "99.96%", response: "226 ms", queue: "18", status: "Sehat" },
    { name: "Integration Worker", type: "Worker", uptime: "99.82%", response: "412 ms", queue: "146", status: "Warning" },
    { name: "Reporting Service", type: "API", uptime: "99.91%", response: "305 ms", queue: "7", status: "Sehat" },
    { name: "Object Storage", type: "Storage", uptime: "99.99%", response: "91 ms", queue: "-", status: "Sehat" }
  ],
  backups: [
    { id: "BKP-001", target: "Database Utama", type: "Full", last: "05 Sep 2026 02:00", size: "184 GB", retention: "30 hari", status: "Berhasil" },
    { id: "BKP-002", target: "Object Storage", type: "Incremental", last: "05 Sep 2026 03:00", size: "42 GB", retention: "14 hari", status: "Berhasil" },
    { id: "BKP-003", target: "Konfigurasi Platform", type: "Snapshot", last: "05 Sep 2026 04:00", size: "2.8 GB", retention: "90 hari", status: "Berhasil" }
  ],
  incidents: [
    { id: "SEC-041", severity: "Medium", title: "Percobaan login berulang", source: "IAM", time: "05 Sep 2026 09:20", status: "Investigasi" },
    { id: "OPS-118", severity: "Low", title: "Antrian integration worker meningkat", source: "Integration", time: "05 Sep 2026 08:55", status: "Monitoring" }
  ]
};

function platformKey() { return `presisiPlatform:${getWorkspace().id}`; }
function getPlatformState() {
  const stored = JSON.parse(localStorage.getItem(platformKey()) || "{}");
  return Object.fromEntries(Object.entries(platformSeed).map(([key, value]) => [key, stored[key] || JSON.parse(JSON.stringify(value))]));
}
const platformMutationRoles = { "master-data": ["adminpusat"], "access-control": ["adminpusat"], organization: ["adminpusat", "adminunit"], personnel: ["adminpusat", "adminunit"], "personnel-detail": ["adminpusat", "adminunit"], reports: ["adminpusat", "adminunit"], operations: ["adminpusat", "adminti"], "system-health": ["adminpusat", "adminti"] };
function canManagePlatformPage(page = document.body.dataset.platformPage, role = getRole()) { return (platformMutationRoles[page] || []).includes(role); }
function savePlatformState(state) { if (!canManagePlatformPage()) { alert("Akses perubahan modul ini tidak tersedia untuk role Anda."); return false; } localStorage.setItem(platformKey(), JSON.stringify(state)); return true; }
function nextPlatformId(prefix, items) { const highest = items.reduce((max, item) => Math.max(max, Number.parseInt(String(item.id).replace(/\D/g, ""), 10) || 0), 0); return `${prefix}-${String(highest + 1).padStart(prefix === "ORG" ? 2 : 3, "0")}`; }
function addOrganizationUnit(state, value) { const name = String(value.name || "").trim(), parent = String(value.parent || "").trim(), leader = String(value.leader || "").trim(), positions = Number(value.positions); if (!name || !parent || !leader || !Number.isFinite(positions) || positions < 1) return { ok: false, message: "Lengkapi unit, parent, pimpinan, dan jumlah jabatan." }; if (state.units.some(item => item.name.toLowerCase() === name.toLowerCase())) return { ok: false, message: "Nama unit sudah digunakan." }; if (parent !== "-" && parent !== "Lemdiklat Polri" && !state.units.some(item => item.name === parent)) return { ok: false, message: "Parent unit tidak ditemukan." }; const item = { id: nextPlatformId("ORG", state.units), name, parent, leader, positions, status: "Aktif" }; state.units.push(item); return { ok: true, item }; }
function addPersonnelRecord(state, value) { const record = Object.fromEntries(Object.entries(value).map(([key, item]) => [key, String(item || "").trim()])); if (!["name", "nrp", "rank", "unit", "position", "competency"].every(key => record[key])) return { ok: false, message: "Seluruh data personel wajib diisi." }; if (state.personnel.some(item => item.nrp === record.nrp)) return { ok: false, message: "NRP/NIP sudah terdaftar." }; const item = { id: nextPlatformId("PER", state.personnel), ...record, status: "Aktif", assignments: [] }; state.personnel.push(item); return { ok: true, item }; }
function assignPersonnelRecord(state, personnelId, value) { const person = state.personnel.find(item => item.id === personnelId); if (!person) return { ok: false, message: "Personel tidak ditemukan." }; const assignment = { id: `ASN-${Date.now()}`, unit: String(value.unit || "").trim(), assignment: String(value.assignment || "").trim(), role: String(value.role || "").trim(), assignedAt: new Date().toISOString() }; if (!assignment.unit || !assignment.assignment || !assignment.role) return { ok: false, message: "Data assignment belum lengkap." }; person.assignments ||= []; if (person.assignments.some(item => item.unit === assignment.unit && item.assignment === assignment.assignment && item.role === assignment.role)) return { ok: false, message: "Assignment yang sama sudah tersedia." }; person.assignments.push(assignment); return { ok: true, item: assignment }; }
function updatePersonnelRecord(state, personnelId, value) { const person = state.personnel.find(item => item.id === personnelId); if (!person) return { ok: false, message: "Personel tidak ditemukan." }; const nrp = String(value.nrp || person.nrp).trim(); if (state.personnel.some(item => item.id !== personnelId && item.nrp === nrp)) return { ok: false, message: "NRP/NIP sudah digunakan personel lain." }; ["name", "nrp", "rank", "unit", "position", "competency", "status"].forEach(key => { if (value[key] !== undefined) person[key] = String(value[key]).trim(); }); return { ok: true, item: person }; }
function addReportSchedule(state, value) { const report = String(value.report || "").trim(), frequency = String(value.frequency || "").trim(), recipient = String(value.recipient || "").trim(); if (!report || !frequency || !recipient) return { ok: false, message: "Laporan, frekuensi, dan penerima wajib diisi." }; if (state.schedules.some(item => item.report === report && item.frequency === frequency && item.recipient.toLowerCase() === recipient.toLowerCase())) return { ok: false, message: "Jadwal laporan yang sama sudah tersedia." }; const item = { id: nextPlatformId("SCH", state.schedules), report, frequency, recipient, next: "Jadwal berikutnya", status: "Aktif" }; state.schedules.push(item); return { ok: true, item }; }
function pBadge(status) {
  const color = /Aktif|Berlaku|Published|Berhasil|Sukses|Sehat/i.test(status) ? "green" : /Draft|Review|Monitoring|Warning/i.test(status) ? "orange" : "red";
  return `<span class="badge ${color}">${escapeHtml(status)}</span>`;
}
function pToast(message) {
  document.querySelector(".toast")?.remove();
  const toast = document.createElement("div"); toast.className = "toast"; toast.textContent = message; document.body.appendChild(toast);
  setTimeout(() => toast.classList.add("show"), 10); setTimeout(() => toast.remove(), 2400);
}
function pModal(title, fields, onSubmit) {
  const layer = document.createElement("div"); layer.className = "modal-layer";
  layer.innerHTML = `<div class="modal-card" role="dialog" aria-modal="true" aria-labelledby="platformModalTitle"><div class="modal-head"><h2 id="platformModalTitle">${escapeHtml(title)}</h2><button class="modal-close" type="button" aria-label="Tutup">×</button></div><form id="platformModalForm"><div class="form-grid">${fields}</div><div class="modal-actions"><button class="btn btn-light modal-cancel" type="button">Batal</button><button class="btn btn-primary" type="submit">Simpan</button></div></form></div>`;
  document.body.appendChild(layer);
  const close = () => layer.remove(); layer.querySelector(".modal-close").onclick = close; layer.querySelector(".modal-cancel").onclick = close;
  layer.addEventListener("click", event => { if (event.target === layer) close(); });
  layer.querySelector("form").onsubmit = event => { event.preventDefault(); onSubmit(new FormData(event.currentTarget)); close(); };
}
function field(label, name, value = "", type = "text", full = false) {
  return `<label class="form-row ${full ? "full" : ""}"><span>${escapeHtml(label)}</span><input class="control" name="${escapeHtml(name)}" type="${type}" value="${escapeHtml(value)}" required></label>`;
}
function selectField(label, name, options, full = false) {
  return `<label class="form-row ${full ? "full" : ""}"><span>${escapeHtml(label)}</span><select class="control" name="${escapeHtml(name)}">${options.map(option => `<option>${escapeHtml(option)}</option>`).join("")}</select></label>`;
}

(function renderPlatformModule() {
  const page = document.body.dataset.platformPage;
  if (!page) return;
  const root = document.querySelector("#platformContent");
  const state = getPlatformState();
  const setup = (active, title, crumb) => { renderSidebar(active); renderHeader(title, crumb); };

  if (page === "master-data") {
    setup("Master Data", "Master Data Nasional", "Pusat / Registry & Governance");
    root.innerHTML = `<div class="hero"><div class="page-actions spread"><div><h1>Master Data Nasional</h1><p>Referensi tunggal untuk organisasi, personel, akademik, capability, dan governance.</p></div><button class="btn btn-light" id="addMaster">+ Tambah Referensi</button></div></div><div class="grid grid-4"><div class="card kpi"><div class="kpi-label">Kelompok Data</div><div class="kpi-value">4</div><div class="kpi-note">Organisasi, personel, akademik, governance</div></div><div class="card kpi"><div class="kpi-label">Referensi Aktif</div><div class="kpi-value">${state.master.filter(x=>x.status==='Berlaku').length}</div><div class="kpi-note">Berlaku nasional</div></div><div class="card kpi"><div class="kpi-label">Menunggu Approval</div><div class="kpi-value">${state.master.filter(x=>x.status!=='Berlaku').length}</div><div class="kpi-note">Maker-checker workflow</div></div><div class="card kpi"><div class="kpi-label">Versi Terakhir</div><div class="kpi-value">v5.0</div><div class="kpi-note">Pangkat Polri</div></div></div><section class="card"><div class="toolbar"><input class="control grow" id="masterSearch" placeholder="Cari nama atau kode..."><select class="control" id="masterGroup"><option value="">Semua kelompok</option>${[...new Set(state.master.map(x=>x.group))].map(x=>`<option>${x}</option>`).join('')}</select></div><div class="table-wrap"><table class="table"><thead><tr><th>ID</th><th>Kelompok</th><th>Referensi</th><th>Versi</th><th>Effective date</th><th>Status</th><th>Aksi</th></tr></thead><tbody id="masterRows"></tbody></table></div></section>`;
    const render = () => { const q=document.querySelector('#masterSearch').value.toLowerCase(),g=document.querySelector('#masterGroup').value;document.querySelector('#masterRows').innerHTML=state.master.filter(x=>(!g||x.group===g)&&`${x.name} ${x.code}`.toLowerCase().includes(q)).map(x=>`<tr><td>${x.id}</td><td>${x.group}</td><td><b>${x.name}</b><br><small class="muted">${x.code}</small></td><td>${x.version}</td><td>${x.effective}</td><td>${pBadge(x.status)}</td><td><button class="text-action" data-approve="${x.id}">${x.status==='Berlaku'?'Lihat':'Approve'}</button></td></tr>`).join('');document.querySelectorAll('[data-approve]').forEach(b=>b.onclick=()=>{const item=state.master.find(x=>x.id===b.dataset.approve);if(item.status!=='Berlaku'){item.status='Berlaku';savePlatformState(state);render();pToast('Master data disetujui dan dipublikasikan.')}else pToast(`Versi ${item.version} aktif sejak ${item.effective}`)})};
    document.querySelector('#masterSearch').oninput=render;document.querySelector('#masterGroup').onchange=render;document.querySelector('#addMaster').onclick=()=>pModal('Tambah Referensi Master',field('Nama referensi','name')+field('Kode','code')+selectField('Kelompok','group',['Organisasi','Personel','Akademik','Governance'])+field('Versi','version','1.0'),data=>{state.master.push({id:`MD-${String(state.master.length+1).padStart(3,'0')}`,name:data.get('name'),code:data.get('code'),group:data.get('group'),version:data.get('version'),effective:new Date().toLocaleDateString('id-ID'),status:'Review'});savePlatformState(state);location.reload()});render();
  }

  if (page === "access-control") {
    setup("User & Hak Akses", "User dan Hak Akses", "Pusat / Security & Governance");
    const roles=['Pimpinan','Admin Pusat','Admin Satdik','Akademik','Gadik','Pengasuh','Peserta','Asesor','Auditor','Admin TI'];
    root.innerHTML=`<div class="hero"><div class="page-actions spread"><div><h1>Identity & Access Management</h1><p>Pengguna, role, permission, data scope, policy, MFA, dan active session.</p></div><button class="btn btn-light" id="addUser">+ Tambah Pengguna</button></div></div><div class="grid grid-4"><div class="card kpi"><div class="kpi-label">Pengguna</div><div class="kpi-value">${state.users.length}</div><div class="kpi-note">Seluruh workspace</div></div><div class="card kpi"><div class="kpi-label">Role</div><div class="kpi-value">11</div><div class="kpi-note">Role prototype</div></div><div class="card kpi"><div class="kpi-label">MFA Aktif</div><div class="kpi-value">${state.users.filter(x=>x.mfa==='Aktif').length}</div><div class="kpi-note">OTP/Authenticator ready</div></div><div class="card kpi"><div class="kpi-label">Policy</div><div class="kpi-value">${state.policies.length}</div><div class="kpi-note">RBAC + ABAC</div></div></div><section class="card"><div class="tabs"><button class="tab active" data-access-tab="users">Pengguna</button><button class="tab" data-access-tab="matrix">Permission Matrix</button><button class="tab" data-access-tab="policy">Policy & Scope</button><button class="tab" data-access-tab="security">MFA & Session</button></div><div id="accessPanel"></div></section>`;
    const panel=document.querySelector('#accessPanel');function renderTab(tab){document.querySelectorAll('[data-access-tab]').forEach(x=>x.classList.toggle('active',x.dataset.accessTab===tab));if(tab==='users')panel.innerHTML=`<div class="table-wrap"><table class="table"><thead><tr><th>Pengguna</th><th>Unit</th><th>Role</th><th>Scope</th><th>MFA</th><th>Status</th><th></th></tr></thead><tbody>${state.users.map(x=>`<tr><td><b>${x.name}</b><br><small class="muted">${x.nrp}</small></td><td>${x.unit}</td><td>${x.role}</td><td>${x.scope}</td><td>${pBadge(x.mfa)}</td><td>${pBadge(x.status)}</td><td><button class="text-action" data-user="${x.id}">Kelola</button></td></tr>`).join('')}</tbody></table></div>`;if(tab==='matrix')panel.innerHTML=`<div class="table-wrap"><table class="table permission-table"><thead><tr><th>Role</th><th>Learning</th><th>Akademik</th><th>Mutu</th><th>LSP</th><th>Integrasi</th><th>Kelola</th></tr></thead><tbody>${roles.map((r,i)=>`<tr><td><b>${r}</b></td>${[0,1,2,3,4].map((_,j)=>`<td><input type="checkbox" ${((i+j)%3)!==0?'checked':''} aria-label="${r} permission ${j+1}"></td>`).join('')}<td><button class="text-action save-permission">Simpan</button></td></tr>`).join('')}</tbody></table></div>`;if(tab==='policy')panel.innerHTML=`<div class="list">${state.policies.map(x=>`<div class="list-item"><span><b>${x.name}</b><small>${x.model} • ${x.rule}</small></span>${pBadge(x.status)}</div>`).join('')}</div>`;if(tab==='security')panel.innerHTML=`<div class="grid grid-3"><div class="card inset"><h3>OTP Email</h3><p class="muted">Simulasikan pengiriman, salah/kedaluwarsa, resend, dan verifikasi.</p><button class="btn btn-secondary security-action">Kirim OTP Demo</button></div><div class="card inset"><h3>Authenticator 2FA</h3><p class="muted">Setup QR dummy, verification code, dan recovery code.</p><button class="btn btn-secondary security-action">Setup 2FA</button></div><div class="card inset"><h3>Active Session</h3><p class="muted">3 sesi aktif • perangkat terakhir Jakarta.</p><button class="btn btn-secondary security-action">Kelola Sesi</button></div></div>`;panel.querySelectorAll('[data-user]').forEach(b=>b.onclick=()=>{const u=state.users.find(x=>x.id===b.dataset.user);pModal('Kelola Pengguna',selectField('Status','status',['Aktif','Suspend'])+selectField('MFA','mfa',['Aktif','Belum'])+selectField('Data scope','scope',['Nasional','SPN-JBR']),data=>{u.status=data.get('status');u.mfa=data.get('mfa');u.scope=data.get('scope');savePlatformState(state);renderTab('users');pToast('Akses pengguna diperbarui.')})});panel.querySelectorAll('.save-permission').forEach(b=>b.onclick=()=>pToast('Permission matrix tersimpan sebagai draft.'));panel.querySelectorAll('.security-action').forEach(b=>b.onclick=()=>pToast(`${b.textContent}: simulasi berhasil.`))}
    document.querySelectorAll('[data-access-tab]').forEach(b=>b.onclick=()=>renderTab(b.dataset.accessTab));document.querySelector('#addUser').onclick=()=>pModal('Tambah Pengguna',field('Nama','name')+field('NRP/NIP','nrp')+field('Unit','unit')+selectField('Role','role',roles),data=>{state.users.push({id:`USR-${String(state.users.length+1).padStart(3,'0')}`,name:data.get('name'),nrp:data.get('nrp'),unit:data.get('unit'),role:data.get('role'),scope:data.get('unit'),mfa:'Belum',status:'Aktif'});savePlatformState(state);location.reload()});renderTab('users');
  }

  if (page === "organization") {
    setup("Struktur Organisasi", "Struktur Organisasi", "Satdik / Organisasi");
    root.innerHTML=`<div class="hero"><div class="page-actions spread"><div><h1>Struktur Organisasi</h1><p>Unit, hubungan parent-child, jabatan, pejabat, dan status organisasi.</p></div><button class="btn btn-light" id="addUnit">+ Tambah Unit</button></div></div><section class="card"><div class="section-title">Peta Organisasi</div><div class="org-tree"><div class="org-node root"><b>Lemdiklat Polri</b><span>Kalemdiklat Polri</span></div><div class="org-branch">${state.units.filter(x=>x.parent==='Lemdiklat Polri').map(x=>`<div class="org-node"><b>${x.name}</b><span>${x.leader}</span></div>`).join('')}</div></div></section><section class="card"><div class="table-wrap"><table class="table"><thead><tr><th>ID</th><th>Unit</th><th>Parent</th><th>Pimpinan</th><th>Jabatan</th><th>Status</th><th></th></tr></thead><tbody>${state.units.map(x=>`<tr><td>${x.id}</td><td><b>${x.name}</b></td><td>${x.parent}</td><td>${x.leader}</td><td>${x.positions}</td><td>${pBadge(x.status)}</td><td><button class="text-action unit-detail" data-id="${x.id}">Detail</button></td></tr>`).join('')}</tbody></table></div></section>`;
    document.querySelector('#addUnit').onclick=()=>pModal('Tambah Unit Organisasi',field('Nama unit','name')+field('Parent unit','parent','Lemdiklat Polri')+field('Nama pimpinan/jabatan','leader')+field('Jumlah jabatan','positions','1','number'),data=>{const result=addOrganizationUnit(state,Object.fromEntries(data));if(!result.ok){alert(result.message);return}if(savePlatformState(state))location.reload()});document.querySelectorAll('.unit-detail').forEach(b=>b.onclick=()=>{const x=state.units.find(u=>u.id===b.dataset.id);pToast(`${x.name}: ${x.positions} jabatan aktif.`)});
  }

  if (page === "personnel") {
    setup("Personel", "Personel", "Satdik / SDM");
    root.innerHTML=`<div class="hero"><div class="page-actions spread"><div><h1>Registry Personel</h1><p>Biodata, unit, jabatan, kompetensi, status, dan assignment personel.</p></div><button class="btn btn-light" id="addPerson">+ Tambah Personel</button></div></div><div class="grid grid-4"><div class="card kpi"><div class="kpi-label">Total Personel</div><div class="kpi-value">${state.personnel.length}</div></div><div class="card kpi"><div class="kpi-label">Aktif</div><div class="kpi-value">${state.personnel.filter(x=>x.status==='Aktif').length}</div></div><div class="card kpi"><div class="kpi-label">Unit</div><div class="kpi-value">${new Set(state.personnel.map(x=>x.unit)).size}</div></div><div class="card kpi"><div class="kpi-label">Kompetensi</div><div class="kpi-value">${new Set(state.personnel.map(x=>x.competency)).size}</div></div></div><section class="card"><div class="toolbar"><input class="control grow" id="personSearch" placeholder="Cari nama, NRP, jabatan..."><select class="control" id="personUnit"><option value="">Semua unit</option>${[...new Set(state.personnel.map(x=>x.unit))].map(x=>`<option>${x}</option>`).join('')}</select></div><div class="table-wrap"><table class="table"><thead><tr><th>Personel</th><th>Pangkat</th><th>Unit/Jabatan</th><th>Kompetensi</th><th>Status</th><th></th></tr></thead><tbody id="personRows"></tbody></table></div></section>`;
    const render=()=>{const q=document.querySelector('#personSearch').value.toLowerCase(),u=document.querySelector('#personUnit').value;document.querySelector('#personRows').innerHTML=state.personnel.filter(x=>(!u||x.unit===u)&&`${x.name} ${x.nrp} ${x.position}`.toLowerCase().includes(q)).map(x=>`<tr><td><b>${x.name}</b><br><small class="muted">NRP/NIP ${x.nrp}</small></td><td>${x.rank}</td><td>${x.unit}<br><small class="muted">${x.position}</small></td><td>${x.competency}</td><td>${pBadge(x.status)}</td><td><a href="personnel-detail.html?id=${x.id}"><b>Lihat Profil →</b></a></td></tr>`).join('')};document.querySelector('#personSearch').oninput=render;document.querySelector('#personUnit').onchange=render;document.querySelector('#addPerson').onclick=()=>pModal('Tambah Personel',field('Nama lengkap','name')+field('NRP/NIP','nrp')+field('Pangkat/Golongan','rank')+field('Unit','unit')+field('Jabatan','position')+field('Kompetensi utama','competency'),data=>{const result=addPersonnelRecord(state,Object.fromEntries(data));if(!result.ok){alert(result.message);return}if(savePlatformState(state))location.reload()});render();
  }

  if (page === "personnel-detail") {
    setup("Personel", "Profil Personel", "Satdik / SDM / Profil");const id=new URLSearchParams(location.search).get('id')||state.personnel[0].id,p=state.personnel.find(x=>x.id===id)||state.personnel[0];
    root.innerHTML=`<div class="profile-hero card"><div class="avatar profile-avatar">${p.name.split(' ').slice(-2).map(x=>x[0]).join('')}</div><div><div class="eyebrow dark">${p.id}</div><h1>${p.name}</h1><p>${p.rank} • ${p.position} • ${p.unit}</p><div class="chip-list"><span>${pBadge(p.status)}</span><span class="badge blue">${p.competency}</span></div></div><div class="page-actions"><button class="btn btn-secondary" id="assignPerson">Assign</button><button class="btn btn-primary" id="editPerson">Ubah Profil</button></div></div><section class="card"><div class="tabs">${['Biodata','Pendidikan','Jabatan','Kompetensi','Prestasi','Penghargaan','Penugasan','Dokumen'].map((x,i)=>`<button class="tab ${i===0?'active':''}" data-profile-tab="${x}">${x}</button>`).join('')}</div><div id="profilePanel"></div></section>`;const details={Biodata:[['NRP/NIP',p.nrp],['Pangkat/Golongan',p.rank],['Status',p.status],['Unit',p.unit]],Pendidikan:[['Pendidikan Polri','Diktuk Bintara Dikreg 62'],['Pendidikan Umum','Magister Manajemen'],['Tahun','2024']],Jabatan:[['Jabatan aktif',p.position],['Unit',p.unit],['TMT','01 Januari 2026']],Kompetensi:[['Kompetensi utama',p.competency],['Sertifikasi','Aktif sampai 2028'],['Level','Madya']],Prestasi:[['Prestasi','Inovasi Pembelajaran Digital'],['Tahun','2025']],Penghargaan:[['Penghargaan','Satyalancana Pengabdian'],['Tahun','2024']],Penugasan:[['Program','Diktuk Bintara Polri'],['Kelas','A-01'],['Peran','Gadik/Reviewer']],Dokumen:[['SK Jabatan','SK-LEMDIKLAT-2026.pdf'],['Sertifikat','CERT-2026-0812.pdf'],['Status','Terverifikasi']]};function tab(name){document.querySelectorAll('[data-profile-tab]').forEach(x=>x.classList.toggle('active',x.dataset.profileTab===name));document.querySelector('#profilePanel').innerHTML=`<div class="detail-grid">${details[name].map(x=>`<div><small>${x[0]}</small><b>${x[1]}</b></div>`).join('')}</div>`}document.querySelectorAll('[data-profile-tab]').forEach(b=>b.onclick=()=>tab(b.dataset.profileTab));document.querySelector('#assignPerson').onclick=()=>pModal('Assignment Personel',selectField('Satdik','unit',['SPN-JBR'])+field('Program/Kelas','assignment','Diktuk Bintara Polri / A-01')+selectField('Peran','role',['Gadik','Reviewer','Pengasuh','Auditor']),()=>pToast('Assignment personel berhasil disimpan.'));document.querySelector('#editPerson').onclick=()=>pToast('Mode ubah profil diaktifkan (simulasi).');tab('Biodata');
  }

  if (page === "reports") {
    setup("Reporting", "Reporting", "Monitoring / Laporan");
    root.innerHTML=`<div class="hero"><div class="page-actions spread"><div><h1>Report Center</h1><p>Katalog, viewer, drill-down, column selector, export, print, dan jadwal laporan.</p></div><button class="btn btn-light" id="scheduleReport">+ Jadwalkan Laporan</button></div></div><div class="grid grid-3">${state.reports.map(x=>`<article class="card report-card"><div class="eyebrow dark">${x.category}</div><h2>${x.name}</h2><p>${x.scope} • ${x.format}</p><small>Diperbarui ${x.updated}</small><button class="btn btn-primary open-report" data-id="${x.id}">Buka Laporan</button></article>`).join('')}</div><section class="card" id="reportViewer"><div class="section-title">Scheduled Report</div><div class="list">${state.schedules.map(x=>`<div class="list-item"><span><b>${x.report}</b><small>${x.frequency} • ${x.recipient} • berikutnya ${x.next}</small></span>${pBadge(x.status)}</div>`).join('')}</div></section>`;
    document.querySelectorAll('.open-report').forEach(b=>b.onclick=()=>{const report=state.reports.find(x=>x.id===b.dataset.id);document.querySelector('#reportViewer').innerHTML=`<div class="page-actions spread"><div><div class="eyebrow dark">Report Viewer</div><h2>${report.name}</h2></div><div class="page-actions"><button class="btn btn-light report-export">Pilih Kolom</button><button class="btn btn-light report-export">Print</button><button class="btn btn-primary report-export">Export</button></div></div><div class="toolbar"><select class="control"><option>Periode: September 2026</option></select><select class="control"><option>Semua Satdik</option><option>SPN-JBR</option></select><button class="btn btn-secondary report-export">Terapkan</button></div><div class="table-wrap"><table class="table"><thead><tr><th>Satdik</th><th>Program</th><th>Peserta</th><th>Kehadiran</th><th>Progress</th><th>Status</th></tr></thead><tbody><tr><td>SPN-JBR</td><td>Diktuk Bintara Polri</td><td>124</td><td>96%</td><td>78%</td><td>${pBadge('On Track')}</td></tr></tbody></table></div>`;document.querySelectorAll('.report-export').forEach(x=>x.onclick=()=>pToast(`${x.textContent} laporan berhasil (simulasi).`));document.querySelector('#reportViewer').scrollIntoView({behavior:'smooth'})});document.querySelector('#scheduleReport').onclick=()=>pModal('Jadwalkan Laporan',selectField('Laporan','report',state.reports.map(x=>x.name))+selectField('Frekuensi','frequency',['Harian','Mingguan','Bulanan'])+field('Penerima','recipient','Pimpinan Satdik'),data=>{state.schedules.push({id:`SCH-${String(state.schedules.length+1).padStart(2,'0')}`,report:data.get('report'),frequency:data.get('frequency'),recipient:data.get('recipient'),next:'Jadwal berikutnya',status:'Aktif'});savePlatformState(state);location.reload()});
  }

  if (page === "audit-log") {
    setup("Audit Log", "Audit Log", "Governance / Audit Trail");
    root.innerHTML=`<div class="hero"><div class="page-actions spread"><div><h1>Audit Trail</h1><p>Jejak login, akses, CRUD, permission, approval, integrasi, dan export.</p></div><button class="btn btn-light" id="exportAudit">Export Audit</button></div></div><div class="grid grid-4"><div class="card kpi"><div class="kpi-label">Event Hari Ini</div><div class="kpi-value">1.842</div></div><div class="card kpi"><div class="kpi-label">Login</div><div class="kpi-value">684</div></div><div class="card kpi"><div class="kpi-label">Perubahan Data</div><div class="kpi-value">327</div></div><div class="card kpi"><div class="kpi-label">Warning</div><div class="kpi-value">12</div></div></div><section class="card"><div class="toolbar"><input class="control grow" id="auditSearch" placeholder="Cari actor, objek, correlation ID..."><select class="control" id="auditModule"><option value="">Semua modul</option>${[...new Set(state.audits.map(x=>x.module))].map(x=>`<option>${x}</option>`).join('')}</select></div><div class="table-wrap"><table class="table"><thead><tr><th>Waktu</th><th>Actor</th><th>Action</th><th>Object/Module</th><th>Result</th><th>Correlation</th><th></th></tr></thead><tbody id="auditRows"></tbody></table></div></section>`;const render=()=>{const q=document.querySelector('#auditSearch').value.toLowerCase(),m=document.querySelector('#auditModule').value;document.querySelector('#auditRows').innerHTML=state.audits.filter(x=>(!m||x.module===m)&&`${x.actor} ${x.object} ${x.correlation}`.toLowerCase().includes(q)).map(x=>`<tr><td>${x.time}</td><td><b>${x.actor}</b><br><small class="muted">${x.ip}</small></td><td>${x.action}</td><td>${x.object}<br><small class="muted">${x.module}</small></td><td>${pBadge(x.result)}</td><td>${x.correlation}</td><td><button class="text-action audit-detail" data-id="${x.id}">Detail</button></td></tr>`).join('');document.querySelectorAll('.audit-detail').forEach(b=>b.onclick=()=>{const x=state.audits.find(a=>a.id===b.dataset.id);pModal('Detail Audit Event',`<div class="full detail-grid">${Object.entries(x).map(([k,v])=>`<div><small>${escapeHtml(k)}</small><b>${escapeHtml(v)}</b></div>`).join('')}<div><small>Before</small><code>{"status":"Draft"}</code></div><div><small>After</small><code>{"status":"Aktif"}</code></div></div>`,()=>pToast('Detail audit ditutup.'))})};document.querySelector('#auditSearch').oninput=render;document.querySelector('#auditModule').onchange=render;document.querySelector('#exportAudit').onclick=()=>pToast('Export audit CSV berhasil (simulasi).');render();
  }

  if (page === "operations") {
    setup("Dashboard Operasional", "Dashboard Operasional TI", "TI / Operations");
    root.innerHTML=`<div class="hero"><div class="page-actions spread"><div><h1>Operations Command Center</h1><p>Service, deployment, queue, storage, security event, dan operational alert.</p></div><button class="btn btn-light" id="runHealth">Run Health Check</button></div></div><div class="grid grid-4"><div class="card kpi"><div class="kpi-label">Service Sehat</div><div class="kpi-value">${state.services.filter(x=>x.status==='Sehat').length}/${state.services.length}</div></div><div class="card kpi"><div class="kpi-label">Uptime</div><div class="kpi-value">99.94%</div></div><div class="card kpi"><div class="kpi-label">Queue</div><div class="kpi-value">171</div></div><div class="card kpi"><div class="kpi-label">Security Event</div><div class="kpi-value">${state.incidents.length}</div></div></div><div class="split-70"><section class="card"><div class="section-title">Service Overview</div><div class="table-wrap"><table class="table"><thead><tr><th>Service</th><th>Tipe</th><th>Uptime</th><th>Response</th><th>Queue</th><th>Status</th></tr></thead><tbody>${state.services.map(x=>`<tr><td><b>${x.name}</b></td><td>${x.type}</td><td>${x.uptime}</td><td>${x.response}</td><td>${x.queue}</td><td>${pBadge(x.status)}</td></tr>`).join('')}</tbody></table></div></section><aside class="card"><div class="section-title">Security & Incident</div><div class="list">${state.incidents.map(x=>`<div class="list-item"><span><b>${x.title}</b><small>${x.id} • ${x.source} • ${x.time}</small></span>${pBadge(x.status)}</div>`).join('')}</div><a class="btn btn-secondary" href="system-health.html">Buka System Health</a></aside></div>`;document.querySelector('#runHealth').onclick=()=>{state.services.forEach(x=>x.status='Sehat');savePlatformState(state);pToast('Health check selesai. Seluruh endpoint merespons.')};
  }

  if (page === "system-health") {
    setup("System Health", "System Health & Recovery", "TI / Reliability");
    root.innerHTML=`<div class="hero"><div class="page-actions spread"><div><h1>System Health & Recovery</h1><p>Uptime, response, queue, storage, backup, restore drill, dan alert.</p></div><button class="btn btn-light" id="restoreDrill">Jalankan Restore Drill</button></div></div><section class="card"><div class="section-title">Health Services</div><div class="service-grid">${state.services.map(x=>`<div class="service-health"><span><b>${x.name}</b><small>${x.type} • ${x.response}</small></span>${pBadge(x.status)}<div class="progress"><i style="width:${parseFloat(x.uptime)}%"></i></div><small>Uptime ${x.uptime} • Queue ${x.queue}</small></div>`).join('')}</div></section><div class="split-60"><section class="card"><div class="section-title">Backup & Recovery</div><div class="table-wrap"><table class="table"><thead><tr><th>Target</th><th>Type</th><th>Last backup</th><th>Size</th><th>Retention</th><th>Status</th></tr></thead><tbody>${state.backups.map(x=>`<tr><td><b>${x.target}</b></td><td>${x.type}</td><td>${x.last}</td><td>${x.size}</td><td>${x.retention}</td><td>${pBadge(x.status)}</td></tr>`).join('')}</tbody></table></div></section><aside class="card"><div class="section-title">Recovery Readiness</div><div class="detail-grid"><div><small>RPO Target</small><b>15 menit</b></div><div><small>RTO Target</small><b>60 menit</b></div><div><small>Restore drill terakhir</small><b id="lastDrill">28 Agu 2026</b></div><div><small>Hasil</small><b class="green-text">Berhasil</b></div></div><div class="alert" style="margin-top:18px">Disaster recovery site siap; replikasi terakhir 4 menit lalu.</div></aside></div>`;document.querySelector('#restoreDrill').onclick=()=>{document.querySelector('#lastDrill').textContent='05 Sep 2026';state.audits.unshift({id:`AUD-${Date.now()}`,time:'05 Sep 2026 18:20',actor:'demo.presisi',action:'RESTORE_DRILL',object:'Database Utama',module:'Operations',ip:'10.20.1.24',result:'Sukses',correlation:'COR-DR01'});savePlatformState(state);pToast('Restore drill berhasil. RTO 42 menit, integritas data valid.')};
  }
})();

const platformPage = document.body.dataset.platformPage, platformRoot = document.querySelector("#platformContent");
if (platformPage && platformMutationRoles[platformPage] && !canManagePlatformPage()) {
  const mutationSelectors = { "master-data": "#addMaster,[data-approve]", "access-control": "#addUser,[data-user],.save-permission,.security-action", organization: "#addUnit", personnel: "#addPerson", "personnel-detail": "#assignPerson,#editPerson", reports: "#scheduleReport", operations: "#runHealth", "system-health": "#restoreDrill" };
  platformRoot?.insertAdjacentHTML("afterbegin", '<div class="alert">Mode baca-saja. Perubahan hanya tersedia untuk role pengelola berwenang.</div>');
  platformRoot?.querySelectorAll(mutationSelectors[platformPage] || "")?.forEach(control => { control.disabled = true; });
}
if (platformPage === "personnel-detail" && canManagePlatformPage()) {
  const personnelState = getPlatformState(), requestedPersonnelId = new URLSearchParams(location.search).get("id"), personnelId = personnelState.personnel.some(item => item.id === requestedPersonnelId) ? requestedPersonnelId : personnelState.personnel[0]?.id;
  document.querySelector("#assignPerson").onclick = () => pModal("Assignment Personel", selectField("Satdik", "unit", ["SPN-JBR"]) + field("Program/Kelas", "assignment", "Diktuk Bintara Polri / A-01") + selectField("Peran", "role", ["Gadik", "Reviewer", "Pengasuh", "Auditor"]), data => { const state = getPlatformState(), result = assignPersonnelRecord(state, personnelId, Object.fromEntries(data)); if (!result.ok) { alert(result.message); return; } if (savePlatformState(state)) pToast("Assignment personel berhasil disimpan."); });
  document.querySelector("#editPerson").onclick = () => { const state = getPlatformState(), person = state.personnel.find(item => item.id === personnelId); pModal("Ubah Profil Personel", field("Nama lengkap", "name", person.name) + field("NRP/NIP", "nrp", person.nrp) + field("Pangkat/Golongan", "rank", person.rank) + field("Unit", "unit", person.unit) + field("Jabatan", "position", person.position) + field("Kompetensi", "competency", person.competency) + selectField("Status", "status", ["Aktif", "Cuti", "Suspend"]), data => { const result = updatePersonnelRecord(state, personnelId, Object.fromEntries(data)); if (!result.ok) { alert(result.message); return; } if (savePlatformState(state)) location.reload(); }); };
}
if (platformPage === "reports") {
  platformRoot?.querySelectorAll(".open-report")?.forEach(button => { const open = button.onclick; button.onclick = () => { open(); document.querySelectorAll("#reportViewer select").forEach((control, index) => control.setAttribute("aria-label", index === 0 ? "Filter periode laporan" : "Filter Satdik laporan")); }; });
  if (canManagePlatformPage()) document.querySelector("#scheduleReport").onclick = () => { const state = getPlatformState(); pModal("Jadwalkan Laporan", selectField("Laporan", "report", state.reports.map(item => item.name)) + selectField("Frekuensi", "frequency", ["Harian", "Mingguan", "Bulanan"]) + field("Penerima", "recipient", "Pimpinan Satdik"), data => { const result = addReportSchedule(state, Object.fromEntries(data)); if (!result.ok) { alert(result.message); return; } if (savePlatformState(state)) location.reload(); }); };
}
