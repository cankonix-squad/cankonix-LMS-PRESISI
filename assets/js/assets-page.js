renderSidebar("Logistic & Asset");
renderHeader("Logistic & Asset", "Operations / Sarpras");
const assetRoot = document.querySelector("#assetContent");
let ops = getOperationsData();
const assetManager = getRole() === "adminunit";
function requireAssetManager(){if(assetManager)return true;showEducationToast("Mode baca-saja: perubahan aset dan logistik tidak diizinkan.");return false}
function applyAssetAccessMode(){if(assetManager)return;assetRoot.insertAdjacentHTML("afterbegin",'<div class="alert">Mode baca-saja. Pengelolaan aset dan logistik memerlukan Admin Satdik.</div>');assetRoot.querySelectorAll('[onclick^="addAsset"],[onclick^="requestLoan"],[onclick^="advanceLoan"],[onclick^="addMaintenance"],[onclick^="advanceMaintenance"],[onclick^="requestDisposal"],[onclick^="advanceDisposal"],[onclick^="stockTransaction"]').forEach(button=>button.remove())}
function assetReload(){ops=getOperationsData();renderAssets();applyAssetAccessMode()}
function money(value){return new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}).format(value)}
function assetName(id){return ops.assets.find(item=>item.id===id)?.name||"-"}
function addAsset(){if(!requireAssetManager())return false;openEducationModal("Registrasi Aset",`<div class="form-grid"><div class="form-row"><label>Kode</label><input name="code" required></div><div class="form-row full"><label>Nama aset</label><input name="name" required></div><div class="form-row"><label>Kategori</label><select name="category">${ops.categories.map(item=>`<option>${escapeHtml(item)}</option>`).join("")}</select></div><div class="form-row"><label>Subkategori</label><input name="subcategory" required></div><div class="form-row"><label>Gedung</label><input name="building" required></div><div class="form-row"><label>Ruang</label><input name="room" required></div><div class="form-row"><label>Nilai aset</label><input name="value" type="number" required></div><div class="form-row"><label>Kondisi</label><select name="condition"><option>Baik</option><option>Perlu Perbaikan</option><option>Rusak</option></select></div></div>`,form=>{const value=Object.fromEntries(form);ops.assets.unshift({id:educationId("asset"),...value,value:Number(value.value),type:"Unit",status:"Available",history:[`${new Date().toISOString().slice(0,10)} • Registrasi aset`]});saveOperationsData(ops);assetReload();showEducationToast("Aset diregistrasi.")});return true}
function showAssetHistory(id){const item=ops.assets.find(asset=>asset.id===id);openEducationModal("Riwayat Aset",`<h2>${escapeHtml(item.name)}</h2><p>${escapeHtml(item.code)} • ${escapeHtml(item.condition)}</p><div class="version-list">${item.history.map(entry=>`<div><b>${escapeHtml(entry)}</b></div>`).join("")}</div>`,()=>false)}
function requestLoan(){if(!requireAssetManager())return false;openEducationModal("Ajukan Peminjaman",`<div class="form-grid"><div class="form-row"><label>Aset</label><select name="assetId">${ops.assets.filter(item=>item.status==="Available").map(item=>`<option value="${item.id}">${escapeHtml(item.name)}</option>`).join("")}</select></div><div class="form-row"><label>Peminjam</label><input name="borrower" required></div><div class="form-row"><label>Mulai</label><input name="start" type="date" required></div><div class="form-row"><label>Jatuh tempo</label><input name="due" type="date" required></div><div class="form-row"><label>Kondisi keluar</label><input name="conditionOut" value="Baik" required></div></div>`,form=>{ops.loans.unshift({id:educationId("loan"),...Object.fromEntries(form),returnDate:"",conditionIn:"",status:"Requested"});saveOperationsData(ops);assetReload()});return true}
function advanceLoan(id){if(!requireAssetManager())return false;const item=ops.loans.find(value=>value.id===id),states=["Requested","Approved","Checked Out","Returned"],index=states.indexOf(item.status);item.status=states[Math.min(states.length-1,index+1)];const asset=ops.assets.find(value=>value.id===item.assetId);if(item.status==="Checked Out")asset.status="In Use";if(item.status==="Returned"){item.returnDate=new Date().toISOString().slice(0,10);item.conditionIn=prompt("Kondisi saat kembali:",item.conditionOut)||item.conditionOut;asset.status="Available";asset.condition=item.conditionIn;asset.history.push(`${item.returnDate} • Dikembalikan oleh ${item.borrower}`)}saveOperationsData(ops);assetReload();return true}
function addMaintenance(){if(!requireAssetManager())return false;openEducationModal("Buat Ticket Maintenance",`<div class="form-grid"><div class="form-row"><label>Aset</label><select name="assetId">${ops.assets.map(item=>`<option value="${item.id}">${escapeHtml(item.name)}</option>`).join("")}</select></div><div class="form-row"><label>Jadwal</label><input name="schedule" type="date" required></div><div class="form-row"><label>Vendor</label><input name="vendor" required></div><div class="form-row"><label>Estimasi biaya</label><input name="cost" type="number" required></div><div class="form-row"><label>Downtime</label><input name="downtime" required></div><div class="form-row full"><label>Masalah</label><textarea name="issue" required></textarea></div></div>`,form=>{const value=Object.fromEntries(form);ops.maintenance.unshift({id:educationId("maint"),...value,cost:Number(value.cost),status:"Scheduled"});saveOperationsData(ops);assetReload()});return true}
function advanceMaintenance(id){if(!requireAssetManager())return false;const item=ops.maintenance.find(value=>value.id===id),asset=ops.assets.find(value=>value.id===item.assetId),states=["Scheduled","In Progress","Completed"],index=states.indexOf(item.status);item.status=states[Math.min(states.length-1,index+1)];asset.status=item.status==="Completed"?"Available":"Maintenance";if(item.status==="Completed"){asset.condition="Baik";asset.history.push(`${new Date().toISOString().slice(0,10)} • Maintenance selesai oleh ${item.vendor}`)}saveOperationsData(ops);assetReload();return true}
function requestDisposal(){if(!requireAssetManager())return false;const assetId=prompt(`ID aset untuk penghapusan:\n${ops.assets.map(item=>`${item.id} — ${item.name}`).join("\n")}`);if(!ops.assets.some(item=>item.id===assetId))return false;const reason=prompt("Alasan penghapusan:");if(!reason)return false;const evidence=prompt("Evidence:","BA Pemeriksaan Aset.pdf")||"Belum ada";ops.disposals.unshift({id:educationId("disp"),assetId,reason,evidence,status:"Requested"});saveOperationsData(ops);assetReload();return true}
function advanceDisposal(id){if(!requireAssetManager())return false;const item=ops.disposals.find(value=>value.id===id),states=["Requested","Approved","Disposed"],index=states.indexOf(item.status);item.status=states[Math.min(states.length-1,index+1)];if(item.status==="Disposed")ops.assets.find(value=>value.id===item.assetId).status="Disposed";saveOperationsData(ops);assetReload();return true}
function stockTransaction(id,type){if(!requireAssetManager())return false;const stock=ops.stock.find(item=>item.id===id),amount=Number(prompt(`Jumlah ${type.toLowerCase()} (${stock.unit}):`,"1"));if(!amount||amount<1)return false;stock.quantity=Math.max(0,stock.quantity+(type==="Return"?amount:-amount));stock.status=stock.quantity<=stock.minimum?"Low Stock":"Available";ops.stockRequests.unshift({id:educationId("stockreq"),stockId:id,requester:roleLabels[getRole()],quantity:amount,type,status:"Completed"});saveOperationsData(ops);assetReload();return true}
function renderAssets(){const totalValue=ops.assets.reduce((sum,item)=>sum+item.value,0),overdue=ops.loans.filter(item=>item.status!=="Returned"&&item.due<new Date().toISOString().slice(0,10)).length;assetRoot.innerHTML=`<div class="hero"><div class="page-actions" style="justify-content:space-between"><div><h1>Logistic & Asset Management</h1><p>Inventaris, ruang, peminjaman, maintenance, disposal, dan stok.</p></div><button class="btn btn-light" onclick="addAsset()">+ Aset</button></div></div><div class="grid grid-5"><div class="card kpi"><div class="kpi-label">Total Asset</div><div class="kpi-value">${ops.assets.length}</div></div><div class="card kpi"><div class="kpi-label">Nilai Aset</div><div class="kpi-value compact-value">${money(totalValue)}</div></div><div class="card kpi"><div class="kpi-label">Available</div><div class="kpi-value">${ops.assets.filter(item=>item.status==="Available").length}</div></div><div class="card kpi"><div class="kpi-label">Maintenance</div><div class="kpi-value">${ops.maintenance.filter(item=>item.status!=="Completed").length}</div></div><div class="card kpi"><div class="kpi-label">Overdue</div><div class="kpi-value">${overdue}</div></div></div><div class="tabs asset-tabs"><a class="tab active" href="#inventory">Inventaris</a><a class="tab" href="#rooms">Ruangan</a><a class="tab" href="#loans">Peminjaman</a><a class="tab" href="#maintenance">Perawatan</a><a class="tab" href="#disposal">Penghapusan</a><a class="tab" href="#stock">Stok</a></div><section class="card" id="inventory"><div class="section-title">Inventaris Aset</div><div class="table-wrap"><table class="table"><thead><tr><th>Aset</th><th>Kategori/Jenis</th><th>Lokasi</th><th>Nilai</th><th>Kondisi</th><th>Status</th><th></th></tr></thead><tbody>${ops.assets.map(item=>`<tr><td><b>${escapeHtml(item.name)}</b><br><small>${escapeHtml(item.code)}</small></td><td>${escapeHtml(item.category)}<br><small>${escapeHtml(item.subcategory)}</small></td><td>${escapeHtml(item.building)}<br><small>${escapeHtml(item.room)}</small></td><td>${money(item.value)}</td><td>${educationBadge(item.condition)}</td><td>${educationBadge(item.status)}</td><td><button class="btn btn-light" onclick="showAssetHistory('${item.id}')">History</button></td></tr>`).join("")}</tbody></table></div></section><section class="card" id="rooms"><div class="section-title">Inventaris Ruangan & Occupancy</div><div class="grid grid-2">${ops.rooms.map(room=>`<div class="room-card"><span><b>${escapeHtml(room.name)}</b><small>${escapeHtml(room.building)} • ${room.occupancy}/${room.capacity} orang</small></span>${educationBadge(room.status)}<div class="progress"><span style="width:${Math.round(room.occupancy/room.capacity*100)}%"></span></div><div class="chip-list">${room.resources.map(resource=>`<span>${escapeHtml(resource)}</span>`).join("")}</div></div>`).join("")}</div></section><section class="card" id="loans"><div class="section-actions"><div class="section-title">Peminjaman</div><button class="btn btn-light" onclick="requestLoan()">+ Peminjaman</button></div>${ops.loans.map(item=>`<div class="ops-row"><span><b>${escapeHtml(assetName(item.assetId))}</b><small>${escapeHtml(item.borrower)} • ${formatDateId(item.start)}–${formatDateId(item.due)} • Keluar: ${escapeHtml(item.conditionOut)}${item.conditionIn?` • Kembali: ${escapeHtml(item.conditionIn)}`:""}</small></span>${educationBadge(item.status)}<button class="btn btn-light" ${item.status==="Returned"?"disabled":""} onclick="advanceLoan('${item.id}')">Lanjut Proses</button></div>`).join("")}</section><section class="card" id="maintenance"><div class="section-actions"><div class="section-title">Perawatan</div><button class="btn btn-light" onclick="addMaintenance()">+ Ticket</button></div>${ops.maintenance.map(item=>`<div class="ops-row"><span><b>${escapeHtml(assetName(item.assetId))}</b><small>${escapeHtml(item.issue)} • ${escapeHtml(item.vendor)} • ${money(item.cost)} • downtime ${escapeHtml(item.downtime)}</small></span>${educationBadge(item.status)}<button class="btn btn-light" ${item.status==="Completed"?"disabled":""} onclick="advanceMaintenance('${item.id}')">Lanjut Proses</button></div>`).join("")}</section><section class="card" id="disposal"><div class="section-actions"><div class="section-title">Penghapusan</div><button class="btn btn-light" onclick="requestDisposal()">+ Request</button></div>${ops.disposals.map(item=>`<div class="ops-row"><span><b>${escapeHtml(assetName(item.assetId))}</b><small>${escapeHtml(item.reason)} • Evidence: ${escapeHtml(item.evidence)}</small></span>${educationBadge(item.status)}<button class="btn btn-light" ${item.status==="Disposed"?"disabled":""} onclick="advanceDisposal('${item.id}')">Approve / Process</button></div>`).join("")}</section><section class="card" id="stock"><div class="section-title">Stok Logistik</div><div class="grid grid-2">${ops.stock.map(item=>`<div class="stock-card"><span><b>${escapeHtml(item.name)}</b><small>Minimum ${item.minimum} ${escapeHtml(item.unit)}</small></span><strong>${item.quantity} ${escapeHtml(item.unit)}</strong>${educationBadge(item.status)}<div class="row-actions"><button onclick="stockTransaction('${item.id}','Issue')">Issue</button><button onclick="stockTransaction('${item.id}','Return')">Return</button></div></div>`).join("")}</div></section>`}
renderAssets();
applyAssetAccessMode();
